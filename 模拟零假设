import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
%matplotlib.pyplot as plt
np.random.seed(42)
df=pd.read_csv('coffee_data.csv')
#提问：喝咖啡的所有人的平均身高是否大于70inch
#零假设：喝咖啡的人平均身高小于等于70
#备择假设：喝咖啡的人平均身高大于70
#若零假设为真，用零假设里最接近对立假设的值进行模拟（70），得到抽样分布的形状。
#用抽样分布的标准偏差确定，如果这个值来自零假设，抽样分布的形状。
#用正态分布进行模拟
sample = df.sample(150)
means=[]
for i in range(10000):
  bootsample = sample.sample(150,replace=True)
  means.append(bootsample.height.mean())
np.std(means)
#抽样分布的标准偏差为0.2658
#根据中心极限定理，抽样分布服从正态分布。
#用70的假设均值和抽样分布标准偏差为0.2658对正态分布中的抽取值进行模拟
null_vals = np.random.normal(70#均值,np.std(means)#标准偏差,10000#size)#零假设中的可能均值 得出的值在69到71
sample.height.mean()
#67.6329 不在零假设的范围内
